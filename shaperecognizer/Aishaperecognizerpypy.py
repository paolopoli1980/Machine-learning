#####################################################################################
############A good suggestion to train the neural network is to use pypy#############
#####################################################################################
import random
import os
def trained_network():

    pass

def training_network(nx,ny,geomfile,activationfunc):
    print (uni_matrix)
    
    


##################################################

choice=input(" 1 - Training\n 2 - Trained\n ")
nx=10
ny=10
numbneurons=100
noutputs=4
activationfunc='elu'
#inputs=np.array(nx*ny)
#outputs=np.array(noutputs)
maxsetmat=2.0
print ('Ready')
if choice=='1':
    print ('hello!')	
    maxsetmax=0 ##########à da settare #############
    #matfirstweights=np.random.rand(nx*ny,numbneurons)*maxsetmat-maxsetmat/2
    #matsecondweights=np.random.rand(numberneurons,noutputs)*maxsetmat-maxsetmat/2
    weights_h = [[random.random() for e in range(nx*ny)] for e in range(numbneurons)] 
    fileslist=[]
    fileslist=os.listdir()
    pnglistfiles=[]
    print (weights_h)



